# =============================================================================
# FinWatch Zambia — SHAP Service (Stub)
# Full SHAP explanation generation wired in Stage 3.
# =============================================================================

# TODO Stage 3: implement compute_shap_values(model, feature_vector)
# Returns dict[str, float] mapping ratio name → SHAP attribution value


def compute_shap_values(model_name: str, feature_vector: list) -> dict:
    raise NotImplementedError("SHAP service will be implemented in Stage 3.")
